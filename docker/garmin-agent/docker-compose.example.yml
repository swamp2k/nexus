services:
  nexus-garmin-agent:
    build:
      context: ../..
      dockerfile: docker/garmin-agent/Dockerfile
    container_name: nexus-garmin-agent
    restart: unless-stopped
    environment:
      TZ: Europe/Copenhagen
      NEXUS_URL: https://nexus.sr-goodjob.workers.dev
      NEXUS_GARMIN_AGENT_TOKEN: ${NEXUS_GARMIN_AGENT_TOKEN}
      NEXUS_GARMIN_STATE_ROOT: /state/users
      NEXUS_GARMIN_DATA_ROOT: /data/users
      NEXUS_GARMIN_POLL_SECONDS: 15
    volumes:
      - /mnt/user/appdata/nexus-garmin/state:/state
      - /mnt/user/appdata/nexus-garmin/data:/data
